.status-pill {
  display: inline-flex;
  align-items: center;
  min-height: 40rpx;
  padding: 0 13rpx;
  border-radius: 999rpx;
  background: var(--surface-red);
  color: var(--xhs-red);
  font-size: 21rpx;
  font-weight: 800;
  line-height: 40rpx;
}
